[pytest]
markers =
    e2e_packaging: End-to-end packaging tests for extension lifecycle and metadata behavior.
    smoke: Fast smoke checks for harness readiness.
    slow: Tests that build and install extensions and can take longer to run.

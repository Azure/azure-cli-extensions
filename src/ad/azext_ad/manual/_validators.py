# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for
# license information.
#
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------


def validate_resource_forest_settings(namespace):
    if not namespace.settings:
        return
    import os
    from azure.cli.core.util import get_file_json, shell_safe_json_parse
    if os.path.exists(namespace.settings):
        namespace.settings = get_file_json(namespace.settings)
    else:
        namespace.settings = shell_safe_json_parse(namespace.settings)


def validate_pfx_certificate(namespace):
    if not namespace.pfx_certificate:
        return
    import os
    from azure.cli.core.util import read_file_content
    if os.path.exists(namespace.pfx_certificate):
        namespace.pfx_certificate = read_file_content(namespace.pfx_certificate, allow_binary=True)

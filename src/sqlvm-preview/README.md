# Azure CLI SQL virtual machine Preview Extension #
This is a extension for SQL virtual machine preview features.

### How to use ###
Install this extension using the below CLI command
```
az extension add --name sqlvm-preview
```

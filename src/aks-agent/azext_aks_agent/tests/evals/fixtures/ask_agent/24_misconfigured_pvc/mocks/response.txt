Redis pod is stuck in Pending state because its PersistentVolumeClaim redis-standard-pvc cannot be provisioned. StorageClass       
standard-v2 does not exist. Fix: create a StorageClass named standard-v2 or update the PVC to use an existing StorageClass.
app-server pod stuck in ContainerCreating because required ConfigMap "app-config" is missing in namespace-79. Pod can't mount      
/config volume.                                                                                                                    

Fix: Create ConfigMap "app-config" in namespace-79 or update pod spec to reference an existing ConfigMap.
[tool.ruff]
extend-exclude = [
  "azext_connectedk8s/vendored_sdks",
]
target-version = "py38"

[tool.ruff.lint]
extend-select = [
  "I",   # isort
  "PIE", # flake8-pie
  "RUF", # ruff
  "SIM", # flake8-simplify
  "UP",  # pyupgrade
]

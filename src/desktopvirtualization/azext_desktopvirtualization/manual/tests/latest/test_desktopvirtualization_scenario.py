# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for
# license information.
#
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------


def call_scenario(test, rg):
    from ....tests.latest import example_steps as g
    # g.setup_scenario(test, rg)
    # g.step_hostpool_create(test, rg, checks=[])
    # g.step_hostpool_show(test, rg, checks=[])
    g.step_hostpool_list(test, rg, checks=[])
    g.step_hostpool_list(test, rg, checks=[])
    # g.step_hostpool_update(test, rg, checks=[])
    # g.step_applicationgroup_create(test, rg, checks=[])
    # g.step_applicationgroup_show(test, rg, checks=[])
    g.step_applicationgroup_list(test, rg, checks=[])
    g.step_applicationgroup_list(test, rg, checks=[])
    # g.step_applicationgroup_update(test, rg, checks=[])
    # g.step_applicationgroup_delete(test, rg, checks=[])
    # g.step_hostpool_delete(test, rg, checks=[])
    # g.step_workspace_create(test, rg, checks=[])
    # g.step_workspace_show(test, rg, checks=[])
    g.step_workspace_list(test, rg, checks=[])
    g.step_workspace_list(test, rg, checks=[])
    # g.step_workspace_update(test, rg, checks=[])
    # g.step_workspace_delete(test, rg, checks=[])
    # g.cleanup_scenario(test, rg)
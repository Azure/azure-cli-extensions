monitor app-insights component favorite create:
  parameters:
    is_generated_from_template:
      rule_exclusions:
      - option_length_too_long
monitor app-insights component favorite update:
  parameters:
    is_generated_from_template:
      rule_exclusions:
      - option_length_too_long

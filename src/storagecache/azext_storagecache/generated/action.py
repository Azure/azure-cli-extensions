# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for
# license information.
#
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------
# pylint: disable=protected-access

import argparse
from collections import defaultdict
from knack.util import CLIError


class AddNetworkSettings(argparse.Action):
    def __call__(self, parser, namespace, values, option_string=None):
        action = self.get_action(values, option_string)
        namespace.network_settings = action

    def get_action(self, values, option_string):  # pylint: disable=no-self-use
        try:
            properties = defaultdict(list)
            for (k, v) in (x.split('=', 1) for x in values):
                properties[k].append(v)
            properties = dict(properties)
        except ValueError:
            raise CLIError('usage error: {} [KEY=VALUE ...]'.format(option_string))
        d = {}
        d['mtu'] = 1500
        d['ntp_server'] = "time.windows.com"
        for k in properties:
            kl = k.lower()
            v = properties[k]
            if kl == 'mtu':
                d['mtu'] = v[0]
            elif kl == 'dns-servers':
                d['dns_servers'] = v
            elif kl == 'dns-search-domain':
                d['dns_search_domain'] = v[0]
            elif kl == 'ntp-server':
                d['ntp_server'] = v[0]
            else:
                raise CLIError('Unsupported Key {} is provided for parameter network_settings. All possible keys are: '
                               'mtu, dns-servers, dns-search-domain, ntp-server'.format(k))
        return d


class AddCredentials(argparse.Action):
    def __call__(self, parser, namespace, values, option_string=None):
        action = self.get_action(values, option_string)
        namespace.credentials = action

    def get_action(self, values, option_string):  # pylint: disable=no-self-use
        try:
            properties = defaultdict(list)
            for (k, v) in (x.split('=', 1) for x in values):
                properties[k].append(v)
            properties = dict(properties)
        except ValueError:
            raise CLIError('usage error: {} [KEY=VALUE ...]'.format(option_string))
        d = {}
        for k in properties:
            kl = k.lower()
            v = properties[k]
            if kl == 'bind-dn':
                d['bind_dn'] = v[0]
            elif kl == 'bind-password':
                d['bind_password'] = v[0]
            else:
                raise CLIError('Unsupported Key {} is provided for parameter credentials. All possible keys are: '
                               'bind-dn, bind-password'.format(k))
        return d


class AddCacheActiveDirectorySettingsCredentials(argparse.Action):
    def __call__(self, parser, namespace, values, option_string=None):
        action = self.get_action(values, option_string)
        namespace.cache_active_directory_settings_credentials = action

    def get_action(self, values, option_string):  # pylint: disable=no-self-use
        try:
            properties = defaultdict(list)
            for (k, v) in (x.split('=', 1) for x in values):
                properties[k].append(v)
            properties = dict(properties)
        except ValueError:
            raise CLIError('usage error: {} [KEY=VALUE ...]'.format(option_string))
        d = {}
        for k in properties:
            kl = k.lower()
            v = properties[k]
            if kl == 'username':
                d['username'] = v[0]
            elif kl == 'password':
                d['password'] = v[0]
            else:
                raise CLIError('Unsupported Key {} is provided for parameter cache_active_directory_settings_credential'
                               's. All possible keys are: username, password'.format(k))
        return d


class AddAccessPolicies(argparse._AppendAction):
    def __call__(self, parser, namespace, values, option_string=None):
        action = self.get_action(values, option_string)
        super(AddAccessPolicies, self).__call__(parser, namespace, action, option_string)

    def get_action(self, values, option_string):  # pylint: disable=no-self-use
        try:
            properties = defaultdict(list)
            for (k, v) in (x.split('=', 1) for x in values):
                properties[k].append(v)
            properties = dict(properties)
        except ValueError:
            raise CLIError('usage error: {} [KEY=VALUE ...]'.format(option_string))
        d = {}
        for k in properties:
            kl = k.lower()
            v = properties[k]
            if kl == 'name':
                d['name'] = v[0]
            elif kl == 'access-rules':
                d['access_rules'] = v
            else:
                raise CLIError('Unsupported Key {} is provided for parameter access_policies. All possible keys are: '
                               'name, access-rules'.format(k))
        return d


class AddJunctions(argparse._AppendAction):
    def __call__(self, parser, namespace, values, option_string=None):
        action = self.get_action(values, option_string)
        super(AddJunctions, self).__call__(parser, namespace, action, option_string)

    def get_action(self, values, option_string):  # pylint: disable=no-self-use
        try:
            properties = defaultdict(list)
            for (k, v) in (x.split('=', 1) for x in values):
                properties[k].append(v)
            properties = dict(properties)
        except ValueError:
            raise CLIError('usage error: {} [KEY=VALUE ...]'.format(option_string))
        d = {}
        d['nfs_access_policy'] = "default"
        for k in properties:
            kl = k.lower()
            v = properties[k]
            if kl == 'namespace-path':
                d['namespace_path'] = v[0]
            elif kl == 'target-path':
                d['target_path'] = v[0]
            elif kl == 'nfs-export':
                d['nfs_export'] = v[0]
            elif kl == 'nfs-access-policy':
                d['nfs_access_policy'] = v[0]
            else:
                raise CLIError('Unsupported Key {} is provided for parameter junctions. All possible keys are: '
                               'namespace-path, target-path, nfs-export, nfs-access-policy'.format(k))
        return d


class AddNfs3(argparse.Action):
    def __call__(self, parser, namespace, values, option_string=None):
        action = self.get_action(values, option_string)
        namespace.nfs3 = action

    def get_action(self, values, option_string):  # pylint: disable=no-self-use
        try:
            properties = defaultdict(list)
            for (k, v) in (x.split('=', 1) for x in values):
                properties[k].append(v)
            properties = dict(properties)
        except ValueError:
            raise CLIError('usage error: {} [KEY=VALUE ...]'.format(option_string))
        d = {}
        for k in properties:
            kl = k.lower()
            v = properties[k]
            if kl == 'target':
                d['target'] = v[0]
            elif kl == 'usage-model':
                d['usage_model'] = v[0]
            else:
                raise CLIError('Unsupported Key {} is provided for parameter nfs3. All possible keys are: target, '
                               'usage-model'.format(k))
        return d


class AddBlobNfs(argparse.Action):
    def __call__(self, parser, namespace, values, option_string=None):
        action = self.get_action(values, option_string)
        namespace.blob_nfs = action

    def get_action(self, values, option_string):  # pylint: disable=no-self-use
        try:
            properties = defaultdict(list)
            for (k, v) in (x.split('=', 1) for x in values):
                properties[k].append(v)
            properties = dict(properties)
        except ValueError:
            raise CLIError('usage error: {} [KEY=VALUE ...]'.format(option_string))
        d = {}
        for k in properties:
            kl = k.lower()
            v = properties[k]
            if kl == 'target':
                d['target'] = v[0]
            elif kl == 'usage-model':
                d['usage_model'] = v[0]
            else:
                raise CLIError('Unsupported Key {} is provided for parameter blob_nfs. All possible keys are: target, '
                               'usage-model'.format(k))
        return d


class AddAttributes(argparse.Action):
    def __call__(self, parser, namespace, values, option_string=None):
        action = self.get_action(values, option_string)
        namespace.attributes = action

    def get_action(self, values, option_string):  # pylint: disable=no-self-use
        try:
            properties = defaultdict(list)
            for (k, v) in (x.split('=', 1) for x in values):
                properties[k].append(v)
            properties = dict(properties)
        except ValueError:
            raise CLIError('usage error: {} [KEY=VALUE ...]'.format(option_string))
        d = {}
        for k in properties:
            v = properties[k]
            d[k] = v[0]
        return d

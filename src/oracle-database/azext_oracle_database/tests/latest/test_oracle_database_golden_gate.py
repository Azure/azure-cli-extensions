# --------------------------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for license information.
#
# Code generated by aaz-dev-tools
# --------------------------------------------------------------------------------------------
import json
import os
import unittest

from azure.cli.testsdk import ScenarioTest, live_only
from azure.cli.testsdk.decorators import serial_test
from azure.cli.testsdk.scenario_tests import AllowLargeResponse


class OracleDatabaseGoldenGateScenario(ScenarioTest):

    @live_only()
    @serial_test()
    @AllowLargeResponse(size_kb=10240)
    def test_oracledatabase_golden_gate_deployment_create_show(self):
        subscription_id = self.get_subscription_id()
        resource_group = os.environ.get(
            'AZURE_ORACLE_DATABASE_GOLDEN_GATE_RESOURCE_GROUP', 'azZCli2026GG'
        )
        location = os.environ.get('AZURE_ORACLE_DATABASE_GOLDEN_GATE_LOCATION', 'eastus')
        resource_anchor_id = os.environ.get(
            'AZURE_ORACLE_DATABASE_GOLDEN_GATE_RESOURCE_ANCHOR_ID',
            '/subscriptions/{}/resourceGroups/{}/providers/Oracle.Database/resourceAnchors/azCLi2026GGRA'.format(
                subscription_id, resource_group
            )
        )
        network_anchor_id = os.environ.get(
            'AZURE_ORACLE_DATABASE_GOLDEN_GATE_NETWORK_ANCHOR_ID',
            '/subscriptions/{}/resourceGroups/{}/providers/Oracle.Database/networkAnchors/azCli2026GGNA'.format(
                subscription_id, resource_group
            )
        )
        admin_username = os.environ.get(
            'AZURE_ORACLE_DATABASE_GOLDEN_GATE_ADMIN_USERNAME', 'AzCliTest'
        )
        admin_password = os.environ.get(
            'AZURE_ORACLE_DATABASE_GOLDEN_GATE_ADMIN_PASSWORD', 'Test##2026'
        )
        deployment_name = self.create_random_name(prefix='ogg', length=20)

        self.cmd(
            'az oracle-database golden-gate-deployment create '
            '--resource-group {} '
            '--name {} '
            '--location {} '
            '--zones 1 '
            '--display-name {} '
            '--resource-anchor-id {} '
            '--network-anchor-id {} '
            '--ogg-data deployment-name={} '
            'admin-username={} '
            'admin-password={} '
            '--no-wait'.format(
                resource_group,
                deployment_name,
                location,
                deployment_name,
                resource_anchor_id,
                network_anchor_id,
                deployment_name,
                admin_username,
                admin_password,
            )
        )
        self.cmd(
            'az oracle-database golden-gate-deployment show '
            '--resource-group {} '
            '--name {}'.format(resource_group, deployment_name)
        )

    # @live_only()
    @serial_test()
    @AllowLargeResponse(size_kb=10240)
    def test_oracledatabase_golden_gate_connection_create_show(self):
        subscription_id = self.get_subscription_id()
        resource_group = os.environ.get(
            'AZURE_ORACLE_DATABASE_GOLDEN_GATE_RESOURCE_GROUP', 'azZCli2026GG'
        )
        location = os.environ.get('AZURE_ORACLE_DATABASE_GOLDEN_GATE_LOCATION', 'eastus')
        resource_anchor_id = os.environ.get(
            'AZURE_ORACLE_DATABASE_GOLDEN_GATE_RESOURCE_ANCHOR_ID',
            '/subscriptions/{}/resourceGroups/{}/providers/Oracle.Database/resourceAnchors/azCLi2026GGRA'.format(
                subscription_id, resource_group
            )
        )
        network_anchor_id = os.environ.get(
            'AZURE_ORACLE_DATABASE_GOLDEN_GATE_NETWORK_ANCHOR_ID',
            '/subscriptions/{}/resourceGroups/{}/providers/Oracle.Database/networkAnchors/azCli2026GGNA'.format(
                subscription_id, resource_group
            )
        )
        database_id = os.environ.get(
            'AZURE_ORACLE_DATABASE_GOLDEN_GATE_ORACLE_DATABASE_ID',
            'ocid1.autonomousdatabase.oc1.iad.anuwcljsnirvylqapizwp5fuzcnc3paah435wuvsmas3363uhraz3hpvk7yq'
        )
        connection_string = os.environ.get(
            'AZURE_ORACLE_DATABASE_GOLDEN_GATE_ORACLE_CONNECTION_STRING', '(description= (retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1521)(host=n53zmqkm.adb.us-ashburn-1.oraclecloud.com))(connect_data=(service_name=g65795d1a5ce1e9_dndadbstets_high.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))'
        )
        username = os.environ.get(
            'AZURE_ORACLE_DATABASE_GOLDEN_GATE_ORACLE_USERNAME', 'admin'
        )
        password_secret_id = os.environ.get(
            'AZURE_ORACLE_DATABASE_GOLDEN_GATE_ORACLE_PASSWORD_SECRET_ID', 'Test##2026'
        )
        oracle = json.dumps({
            'technology-type': 'ORACLE_DATABASE',
            'database-id': database_id,
            'connection-string': connection_string,
            'username': username,
            'password-secret-id': password_secret_id,
        }).replace('{', '{{').replace('}', '}}')
        connection_name = self.create_random_name(prefix='ogg', length=20)

        self.cmd(
            'az oracle-database golden-gate-connection create '
            '--resource-group {} '
            '--name {} '
            '--location {} '
            '--zones 1 '
            '--display-name {} '
            '--resource-anchor-id {} '
            '--network-anchor-id {} '
            '--does-use-secret-ids false '
            "--oracle '{}' "
            '--no-wait'.format(
                resource_group,
                connection_name,
                location,
                connection_name,
                resource_anchor_id,
                network_anchor_id,
                oracle,
            )
        )
        self.cmd(
            'az oracle-database golden-gate-connection show '
            '--resource-group {} '
            '--name {}'.format(resource_group, connection_name)
        )


if __name__ == '__main__':
    unittest.main()

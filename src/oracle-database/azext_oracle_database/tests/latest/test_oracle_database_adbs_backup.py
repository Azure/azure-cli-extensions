# --------------------------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for license information.
#
# Code generated by aaz-dev-tools
# --------------------------------------------------------------------------------------------
import os
import unittest
import time

from azure.cli.testsdk.scenario_tests import AllowLargeResponse
from azure.cli.testsdk import (ScenarioTest, ResourceGroupPreparer, live_only)


class OracleDatabaseAdbsBackupScenario(ScenarioTest):
    @live_only()
    @AllowLargeResponse(size_kb=10240)
    @ResourceGroupPreparer(name_prefix='cli_test_odba_rg')
    def test_oracledatabase_adbs_backup_commands(self, resource_group):
        resource_group_name = os.environ.get('AZURE_ORACLE_DATABASE_ADBS_RESOURCE_GROUP', 'PowerShellTestRg')
        autonomous_database_name = os.environ.get('AZURE_ORACLE_DATABASE_ADBS_NAME', 'ADBScli1')
        backup_name = os.environ.get('AZURE_ORACLE_DATABASE_ADBS_BACKUP_NAME', 'ADBScliBackupTest')
        backup_display_name = os.environ.get('AZURE_ORACLE_DATABASE_ADBS_BACKUP_DISPLAY_NAME', backup_name)
        retention_period_in_days = os.environ.get('AZURE_ORACLE_DATABASE_ADBS_BACKUP_RETENTION_DAYS', '60')
        updated_retention_period_in_days = os.environ.get('AZURE_ORACLE_DATABASE_ADBS_BACKUP_UPDATED_RETENTION_DAYS', '61')

        self.cmd('az oracle-database autonomous-database backup create '
                 '--resource-group {} '
                 '--autonomousdatabasename {} '
                 '--adbbackupid {} '
                 '--display-name {} '
                 '--retention-period-in-days {} '.format(
                     resource_group_name,
                     autonomous_database_name,
                     backup_name,
                     backup_display_name,
                     retention_period_in_days
                 ))

        self.cmd('az oracle-database autonomous-database backup list '
                 '--resource-group {} '
                 '--autonomousdatabasename {} '.format(resource_group_name, autonomous_database_name))

        self.cmd('az oracle-database autonomous-database backup show '
                 '--resource-group {} '
                 '--autonomousdatabasename {} '
                 '--adbbackupid {} '.format(resource_group_name, autonomous_database_name, backup_name))

        self.cmd('az oracle-database autonomous-database backup wait '
                 '--resource-group {} '
                 '--autonomousdatabasename {} '
                 '--adbbackupid {} '
                 '--created '.format(resource_group_name, autonomous_database_name, backup_name))

        self.cmd('az oracle-database autonomous-database backup update '
                 '--resource-group {} '
                 '--autonomousdatabasename {} '
                 '--adbbackupid {} '
                 '--retention-period-in-days {} '.format(
                     resource_group_name,
                     autonomous_database_name,
                     backup_name,
                     updated_retention_period_in_days
                 ))

        self.cmd('az oracle-database autonomous-database backup delete '
                 '--resource-group {} '
                 '--autonomousdatabasename {} '
                 '--adbbackupid {} '
                 '--yes --no-wait '.format(resource_group_name, autonomous_database_name, backup_name))


if __name__ == '__main__':
    unittest.main()

info:
  name: ListRunbooks
  type: http
  seq: 4

http:
  method: GET
  url: https://management.azure.com/{{MigrateProjectResourceId}}/runbooks?api-version={{RunbooksAPIVersion}}
  params:
    - name: api-version
      value: "{{RunbooksAPIVersion}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for
# license information.
#
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------

from azure.ai.ml.constants._common import YAMLRefDocLinks

from ..custom.utils import private_features_enabled
from ._common_params import (
    add_archived_only_param,
    add_common_params,
    add_file_param,
    add_include_archived_param,
    add_max_results_params,
    add_override_param,
    add_registry_param,
    add_skip_validation_params,
)


def add_name_param(c):
    c.argument("name", options_list=["--name", "-n"], type=str, help="Name of the component.")


def add_version_param(c):
    c.argument("version", options_list=["--version", "-v"], type=str, help="Version of the component.")


def add_label_param(c):
    c.argument("label", options_list=["--label", "-l"], type=str, help="Label of the component.")


def load_component_params(self):
    with self.argument_context("ml component list") as c:
        add_common_params(c)
        add_version_param(c)
        add_name_param(c)
        add_max_results_params(c)
        add_include_archived_param(c, help_message="List archived components and active components.")
        add_archived_only_param(c, help_message="List archived components only.")
        add_registry_param(c)

    with self.argument_context("ml component show") as c:
        add_common_params(c)
        add_name_param(c)
        add_version_param(c)
        add_label_param(c)
        add_registry_param(c)

    with self.argument_context("ml component create") as c:
        add_override_param(c)
        add_common_params(c)
        add_version_param(c)
        add_registry_param(c)
        add_name_param(c)
        add_skip_validation_params(c)
        add_file_param(c, "component", YAMLRefDocLinks.COMMAND_COMPONENT)

    if private_features_enabled():
        with self.argument_context("ml component validate") as c:
            add_common_params(c)
            add_file_param(c, "component", YAMLRefDocLinks.COMMAND_COMPONENT)

    with self.argument_context("ml component update") as c:
        add_common_params(c)
        add_name_param(c)
        add_version_param(c)
        add_label_param(c)
        add_registry_param(c)

    with self.argument_context("ml component archive") as c:
        add_common_params(c)
        add_name_param(c)
        add_version_param(c)
        add_label_param(c)
        add_registry_param(c)

    with self.argument_context("ml component restore") as c:
        add_common_params(c)
        add_name_param(c)
        add_version_param(c)
        add_label_param(c)
        add_registry_param(c)

    with self.argument_context("ml component prepare-for-sign") as c:
        add_common_params(c)
        add_registry_param(c)
        add_file_param(c, "component", YAMLRefDocLinks.COMMAND_COMPONENT)

# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for
# license information.
#
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------

from azure.ai.ml.constants._common import YAMLRefDocLinks
from ._common_params import (
    add_common_params,
    add_lro_param,
    add_file_param,
    add_override_param,
)


def add_name_param(c):
    c.argument(
        "name",
        options_list=["--name", "-n"],
        type=str,
        help="Name of the capability host.",
    )


def add_capability_host_common_params(c):
    c.argument(
        "description",
        options_list=["--description"],
        type=str,
        help="Description of the capability host.",
    )
    c.argument(
        "vector_store_connections",
        options_list=["--vector-store-connections", "-v"],
        help="List of vector store (AISearch) connections names.",
    )
    c.argument(
        "storage_connections",
        options_list=["--storage-connections", "-s"],
        help="List of storage connections names.",
    )
    c.argument(
        "ai_services_connections",
        options_list=["--ai-services-connections", "-a"],
        help="List of Open AIServices connections names.",
    )
    c.argument(
        "capability_host_kind",
        options_list=["--capability-host-kind"],
        type=str,
        help="Capability host kind. Default value is Agents.",
    )


def load_capability_host_params(self):
    with self.argument_context("ml capability-host create") as c:
        add_common_params(c)
        add_name_param(c)
        add_capability_host_common_params(c)
        add_file_param(c, "capability-host", YAMLRefDocLinks.CAPABILITY_HOST)
        add_override_param(c)
        add_lro_param(c)

    with self.argument_context("ml capability-host show") as c:
        add_common_params(c)
        add_name_param(c)

    with self.argument_context("ml capability-host delete") as c:
        add_common_params(c)
        add_name_param(c)
        add_lro_param(c)

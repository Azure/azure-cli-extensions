# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for
# license information.
#
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------
from azext_mlv2.manual._params._common_params import (
    add_common_params,
    add_file_param,
    add_max_results_params,
    add_override_param,
)

from azure.ai.ml.constants._common import YAMLRefDocLinks


def add_name_param(c):
    c.argument("name", options_list=["--name", "-n"], type=str, help="Name of the schedule.")


def add_list_view_param(c):
    c.argument(
        "include_disabled",
        options_list=["--include-disabled"],
        help="List disabled schedules and enabled schedules.",
    )
    c.argument(
        "disabled_only",
        options_list=["--disabled-only"],
        help="List disabled schedules only.",
    )


def add_job_status_param(c, help_message: str):
    c.argument(
        "job_status",
        options_list=["--job-status"],
        help=help_message,
    )


def load_schedule_params(self):
    with self.argument_context("ml schedule list") as c:
        add_common_params(c)
        add_max_results_params(c)
        add_list_view_param(c)

    with self.argument_context("ml schedule show") as c:
        add_common_params(c)
        add_name_param(c)

    with self.argument_context("ml schedule create") as c:
        add_override_param(c)
        add_common_params(c)
        add_name_param(c)
        add_file_param(c, "schedule", YAMLRefDocLinks.JOB_SCHEDULE)

    with self.argument_context("ml schedule update") as c:
        add_common_params(c)
        add_name_param(c)

    with self.argument_context("ml schedule disable") as c:
        add_common_params(c)
        add_name_param(c)

    with self.argument_context("ml schedule enable") as c:
        add_common_params(c)
        add_name_param(c)

    with self.argument_context("ml schedule delete") as c:
        add_common_params(c)
        add_name_param(c)

    with self.argument_context("ml schedule trigger") as c:
        add_common_params(c)
        add_name_param(c)

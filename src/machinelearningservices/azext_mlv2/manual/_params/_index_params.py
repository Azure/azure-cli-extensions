# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for
# license information.
#
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------


from ._common_params import (
    add_common_params,
    add_datastore_param,
    add_description_param,
    add_file_param,
    add_max_results_params,
    add_override_param,
    add_registry_param,
    add_stage_param,
    add_tags_param,
)


def add_version_param(c):
    c.argument("version", options_list=["--version", "-v"], type=str, help="Version of the index.")


def add_name_param(c):
    c.argument("name", options_list=["--name", "-n"], type=str, help="Name of the index.")


def add_label_param(c):
    c.argument("label", options_list=["--label", "-l"], help="Label of the index.")


def load_index_params(self):
    with self.argument_context("ml index create") as c:
        add_common_params(c)
        add_override_param(c)
        add_tags_param(c)
        add_name_param(c)
        add_version_param(c)
        add_registry_param(c)
        add_file_param(c, "index", "")
        add_datastore_param(c)
        add_stage_param(c, asset_name="index")
        c.argument(
            "path",
            options_list=["--path", "-p"],
            help="Path to the index file(s). This can be either a local or a remote location. "
                 "If specified, --name/-n must also be provided.",
        )
        add_description_param(c, help_message="Description of the index.")

    with self.argument_context("ml index show") as c:
        add_common_params(c)
        add_name_param(c)
        add_version_param(c)
        add_label_param(c)
        add_registry_param(c)

    with self.argument_context("ml index list") as c:
        add_common_params(c)
        c.argument(
            "name",
            options_list=["--name", "-n"],
            help="Name of the index. If provided, all the index versions under this name will be returned.",
        )
        add_stage_param(c, asset_name="index")
        add_max_results_params(c)
        add_registry_param(c)

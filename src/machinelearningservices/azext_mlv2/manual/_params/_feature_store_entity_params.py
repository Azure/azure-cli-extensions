# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for
# license information.
#
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------

from azure.ai.ml.constants._common import YAMLRefDocLinks

from ._common_params import (
    add_archived_only_param,
    add_description_param,
    add_feature_store_common_params,
    add_file_param,
    add_include_archived_param,
    add_max_results_params,
    add_override_param,
)


def add_name_param(c):
    c.argument("name", options_list=["--name", "-n"], type=str, help="Name of the feature store entity.")


def add_version_param(c):
    c.argument("version", options_list=["--version", "-v"], help="Version of the feature store entity.")


def load_feature_store_entity_params(self):
    with self.argument_context("ml feature-store-entity list") as c:
        add_feature_store_common_params(c)
        c.argument(
            "name",
            options_list=["--name", "-n"],
            type=str,
            help="Name of the feature store entity. If provided, all the feature store entity versions "
                 "under this name will be returned.",
        )
        add_include_archived_param(
            c, help_message="List archived feature store entity assets and active feature store entity assets."
        )
        add_archived_only_param(c, help_message="List archived feature store entity assets only.")
        add_max_results_params(c)

    with self.argument_context("ml feature-store-entity show") as c:
        add_feature_store_common_params(c)
        add_name_param(c)
        add_version_param(c)

    with self.argument_context("ml feature-store-entity create") as c:
        add_feature_store_common_params(c)
        add_override_param(c)
        add_name_param(c)
        add_version_param(c)
        add_description_param(c, help_message="Description of the feature store entity.")
        c.argument("description", options_list=["--description", "-d"], help="Description of the feature store entity.")
        add_file_param(c, "feature-store-entity", YAMLRefDocLinks.FEATURE_STORE_ENTITY)
        c.argument("no_wait", help="Do not wait for feature store entity creation to finish.")

    with self.argument_context("ml feature-store-entity update") as c:
        add_feature_store_common_params(c)
        add_name_param(c)
        add_version_param(c)
        c.argument("no_wait", help="Do not wait for feature store entity update to finish.")

    with self.argument_context("ml feature-store-entity archive") as c:
        add_feature_store_common_params(c)
        add_name_param(c)
        add_version_param(c)

    with self.argument_context("ml feature-store-entity restore") as c:
        add_feature_store_common_params(c)
        add_name_param(c)
        add_version_param(c)
